# Help images

Place optimized screenshots and workflow illustrations in this directory.

Use descriptive lowercase filenames, include meaningful alternative text in
`manual.md`, crop away unrelated desktop content, and avoid confidential drawing
names or paths. Prefer WebP or optimized PNG and keep each image at the smallest
resolution that remains legible on a high-density display.

